<h1>Edit profile</h1>

<% render partial: "errors" %>

<%= form_for @user do |from| %>
        <% render partial: "fields", locals: { f: from } %>

        <%= form.submit 'Update user profile' %>
<% end %>